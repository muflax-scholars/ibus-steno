ibus-steno -- a Steno input engine for IBus

Based on [Plover](http://ploversteno.org/).

* Installation

- `git clone git@github.com:muflax/ibus-steno.git`
- `cd ibus-steno`
- `./autogen.sh`
- `./configure --prefix=/usr`
- `make`
- `make install`
- Enable ibus-steno in IBus through `ibus-setup`. The engine is under "English" and called "steno".
